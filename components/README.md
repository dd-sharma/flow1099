# Components

Reusable React components for the Flow1099 application.

This directory contains all shared components used across the application, including:
- UI components from shadcn/ui (in the `ui/` subdirectory)
- Marketing components (in the `marketing/` subdirectory)
- Navigation and theme components
